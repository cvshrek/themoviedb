import { Colors, Dimens } from "@enums";
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  text: {
    fontSize: Dimens.dimen_16,
    color: Colors.black,
  },
});

export default styles;
